# gunicorn.conf (or similar configuration file)
bind = '0.0.0.0:5000'  # Change port if needed
workers = 3  # Adjust the number of worker processes
wsgi_app = 'your_app:app'  # Replace with your app's name and object